{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "lerna run --scope @fio/* --parallel start",
    "build": "lerna run --scope @fio/* build",
    "serve": "lerna run --scope @fio/* --parallel serve",
    "clean": "lerna run --scope @fio/* --parallel clean"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
